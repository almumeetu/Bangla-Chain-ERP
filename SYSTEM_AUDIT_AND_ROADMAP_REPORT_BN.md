# 🏢 বাংলা-চেইন ইআরপি (Bangla-Chain ERP) — পূর্ণাঙ্গ সিস্টেম রিভিউ, আর্কিটেকচার অডিট ও রোডম্যাপ রিপোর্ট

**তারিখ:** ২৯ আগস্ট, ২০২৬  
**প্রজেক্ট নাম:** Bangla-Chain ERP (FMCG Dealer & Distribution Management System)  
**ভার্সন:** ১.০.০  
**স্ট্যাক:** Next.js (App Router), React 19, TypeScript, Tailwind CSS, Supabase (PostgreSQL + RLS + Auth), Resend API, jsPDF  

---

## 📑 সূচিপত্র
1. [নির্বাহী সারসংক্ষেপ (Executive Summary)](#১-নির্বাহী-সারসংক্ষেপ-executive-summary)
2. [বর্তমান সিস্টেম আর্কিটেকচার ও ডেটা ফ্লো](#২-বর্তমান-সিস্টেম-আর্কিটেকচার-ও-ডেটা-ফ্লো)
3. [বর্তমান সিস্টেমের শক্তিশালী দিকগুলো (Strengths)](#৩-বর্তমান-সিস্টেমের-শক্তিশালী-দিকগুলো-strengths)
4. [চিহ্নিত সমস্যা ও টেকনিক্যাল সীমাবদ্ধতা (Identified Bottlenecks & Critical Issues)](#৪-চিহ্নিত-সমস্যা-ও-টেকনিক্যাল-সীমাবদ্ধতা-identified-bottlenecks--critical-issues)
5. [ব্যাকএন্ড ও বিজনেস লজিক কেমন হওয়া উচিত (Ideal Backend & Architecture Design)](#৫-ব্যাকএন্ড-ও-বিজনেস-লজিক-কেমন-হওয়া-উচিত-ideal-backend--architecture-design)
6. [মডিউলভিত্তিক বিস্তারিত রিভিউ (Module-by-Module Audit)](#৬-মডিউলভিত্তিক-বিস্তারিত-রিভিউ-module-by-module-audit)
7. [ডেটাবেজ ও সিকিউরিটি অপ্টিমাইজেশন প্ল্যান](#৭-ডেটাবেজ-ও-সিকিউরিটি-অপ্টিমাইজেশন-প্ল্যান)
8. [পরবর্তী কর্মপরিকল্পনা ও রোডম্যাপ (Implementation Roadmap)](#৮-পরবর্তী-কর্মপরিকল্পনা-ও-রোডম্যাপ-implementation-roadmap)

---

## ১. নির্বাহী সারসংক্ষেপ (Executive Summary)

**Bangla-Chain ERP** হলো বাংলাদেশের ডিলার, পাইকারি ও ডিস্ট্রিবিউশন ব্যবসার জন্য বিশেষভাবে ডিজাইন করা একটি ক্লাউড-ভিত্তিক এন্টারপ্রাইজ রিসোর্স প্ল্যানিং (ERP) সফটওয়্যার। সফটওয়্যারটিতে ডিলার ব্যবসার বাস্তব চ্যালেঞ্জ—যেমন কার্টন ও পিস কনভার্শন, বোনাস প্রডাক্ট ম্যানেজমেন্ট, এসআর (SR) কমিশন হিসাব, মার্কেট রুট ডেলিভারি এবং ড্যামেজ ক্লেইম সেটেলমেন্ট অত্যন্ত নিখুঁতভাবে সমন্বয় করা হয়েছে।

### বর্তমান অবস্থা:
- UI/UX ডিজাইন খুবই চমৎকার, পরিষ্কার এবং সম্পূর্ণ বাংলায় ও ইংরেজিতে কাজ করতে সক্ষম।
- অধিকাংশ বিজনেস ফাংশনালিটি (চালান, প্রকিউরমেন্ট, স্টক অ্যাডজাস্টমেন্ট, এক্সপেন্স, কাস্টমার বাকি ট্র্যাকিং) ফ্রন্টএন্ডে সফলভাবে রান করছে।
- **তবে,** অ্যাপ্লিকেশনটি বর্তমানে একটি **"Client-Heavy In-Memory"** আর্কিটেকচারে চলছে। অর্থাৎ, ডাটাবেজ থেকে সমস্ত ডেটা একবারে ব্রাউজারের মেমোরিতে এনে ফ্রন্টএন্ড স্টেট দিয়ে স্টক ক্যালকুলেশন ও আপডেট করা হচ্ছে। এন্টারপ্রাইজ স্কেলে এটি ডাটা মিসম্যাচ, রেস কন্ডিশন ও পারফরম্যান্স স্লো হওয়ার ঝুঁকি তৈরি করে।

---

## ২. বর্তমান সিস্টেম আর্কিটেকচার ও ডেটা ফ্লো

```
┌────────────────────────────────────────────────────────────────────────┐
│                        Next.js Frontend (React 19)                     │
│  ┌───────────────────────┐   ┌─────────────────┐   ┌────────────────┐  │
│  │   Dashboard / UI      │   │  Challan / Sell │   │ Reports Engine │  │
│  └───────────┬───────────┘   └────────┬────────┘   └────────┬───────┘  │
│              │                        │                     │          │
│              └────────────────────────┼─────────────────────┘          │
│                                       ▼                                │
│                           useErpData State Hook                        │
│                           (loadAllData on mount)                       │
│                                       │                                │
│                         makeSyncer (Diff & Sync)                       │
└───────────────────────────────────────┼────────────────────────────────┘
                                        │ (Direct Supabase Client Calls)
                                        ▼
┌────────────────────────────────────────────────────────────────────────┐
│                     Supabase Backend (PostgreSQL)                      │
│   - Tables with RLS (owner_id = auth.uid())                            │
│   - Auth: Admin (Supabase Auth) vs SR (Custom Password in Table)       │
│   - Storage: Logo / Attachment Buckets                                 │
└────────────────────────────────────────────────────────────────────────┘
```

---

## ৩. বর্তমান সিস্টেমের শক্তিশালী দিকগুলো (Strengths)

1. **বাংলাদেশের ডিস্ট্রিবিউশন ব্যবসার সাথে শতভাগ মানানসই:**
   - কার্টন এবং পিস সাইজের স্বয়ংক্রিয় কনভার্শন (যেমন: ১ কার্টন = ২৪ পিস)।
   - চালানে ফ্রি বোনাস পিস হ্যান্ডেল করার সুনির্দিষ্ট লজিক।
   - কোম্পানিভিত্তিক এসআর (SR) অ্যাসাইনমেন্ট এবং রুট প্ল্যানিং।
2. **সম্পূর্ণ বাংলা ও ইংরেজি সাপোর্ট (Bilingual Localization):**
   - `translations.ts` এর মাধ্যমে খুব সহজে এক ক্লিকেই পুরো ইন্টারফেস বাংলা থেকে ইংরেজিতে পরিবর্তন করা যায়।
3. **প্রফেশনাল প্রিন্ট ও পিডিএফ এক্সপোর্ট ইঞ্জিন:**
   - ইনভয়েস, ডেলিভারি চালান শিট, প্রকিউরমেন্ট ভাউচার এবং স্টক ভ্যালুয়েশন প্রিন্ট ফরম্যাট তৈরি করা আছে।
   - `reportEngine.ts` এবং `generatePDF.ts` দিয়ে ক্লায়েন্ট-সাইড থেকেই দ্রুত A4 সাইজ রিপোর্ট জেনারেট করা সম্ভব।
4. **ইনভয়েস ইমেইলিং সুবিধা:**
   - Resend API এর মাধ্যমে স্বয়ংক্রিয়ভাবে কাস্টমারের কাছে এইচটিএমএল (HTML) ইনভয়েস মেইল পাঠানোর ব্যবস্থা রয়েছে।

---

## ৪. চিহ্নিত সমস্যা ও টেকনিক্যাল সীমাবদ্ধতা (Identified Bottlenecks & Critical Issues)

### 🔴 ১. মেমোরি ওভারলোড ও স্কেলেবিলিটি রিস্ক (`loadAllData`)
- **সমস্যা:** অ্যাপ্লিকেশন লোড হওয়ার সময় `loadAllData()` একসাথে ২০+ টেবিলের সমস্ত ডাটা (হাজার হাজার প্রোডাক্ট, চালান, হিস্ট্রি) ব্রাউজারের মেমোরিতে নিয়ে আসে।
- **ঝুঁকি:** একটি ডিলার প্রতিষ্ঠানে ১ বছরে ১০,০০০+ চালান তৈরি হলে মোবাইল বা সাধারণ পিসিতে অ্যাপ লোড হতে ২০-৩০ সেকেন্ড সময় নিবে অথবা মেমোরি ক্র্যাশ করবে।
- **সমাধান:** সার্ভার-সাইড পেজিনেশন (Pagination) ও ডেট-রেঞ্জ কোয়েরি চালু করতে হবে।

### 🔴 ২. স্টক ও চালানে রেস কন্ডিশন (Race Conditions in Stock Update)
- **সমস্যা:** স্টক বিয়োগ বা যোগ করার লজিক বর্তমানে ফ্রন্টএন্ড কোডে (`ChallanModule.tsx` এর `executeTransaction`) চলছে।
- **ঝুঁকি:** যদি একই সাথে দুজন এসআর (SR) বা অ্যাডমিন একটি প্রোডাক্ট বিক্রি করেন (যার স্টক আছে মাত্র ১০টি, অথচ দুইজনই ১০টি করে বিক্রি অর্ডার সাবমিট করলেন), ফ্রন্টএন্ড স্টেট দুটো অর্ডারই কনফার্ম করে দেবে এবং ডাটাবেজের স্টক নেগেটিভ হয়ে যাবে।
- **সমাধান:** স্টক কাটার কাজ ডেটাবেজ লেভেলে **PostgreSQL Transaction (RPC / Stored Procedure)** দিয়ে অ্যাটমিক (Atomic) করতে হবে।

### 🟠 ৩. আন-হ্যান্ডেল্ড ডেটা সিঙ্ক (`makeSyncer` Fire-and-Forget)
- **সমস্যা:** `useErpData.ts` ফাইলে `makeSyncer` ফাংশন ডাটাবেজে আপডেট পাঠানোর সময় `.catch(console.error)` ব্যবহার করে ফায়ার-এন্ড-ফরগেট পদ্ধতিতে চলে।
- **ঝুঁকি:** ইন্টারনেট ডিসকানেক্ট হলে বা ডাটাবেজ টাইম-আউট হলে ব্রাউজারে দেখাবে ডাটা সেভ হয়েছে, কিন্তু রিলোড দিলে আগের ডাটা ফিরে আসবে (Silent Data Loss)।
- **সমাধান:** অপটিমিস্টিক মিউটেশন উইথ রোলব্যাক (React Query / SWR / Server Actions) ব্যবহার করা।

### 🟠 ৪. এসআর (SR) অথেন্টিকেশন ও সিকিউরিটি রিস্ক
- **সমস্যা:** `srs` টেবিলে `login_password` প্লেইন টেক্সট আকারে সেভ আছে (কোনো পাসওয়ার্ড হ্যাশিং নেই)। এছাড়া RLS অন থাকায় আন-অথেন্টিকেটেড ইউজার সরাসরি `srs` টেবিলে কোয়েরি করতে পারে না।
- **সমাধান:** পাসওয়ার্ড bcrypt দিয়ে হ্যাশ করতে হবে এবং একটি নিরাপদ Next.js API রুট (`/api/auth/sr-login`) দিয়ে সেশন হ্যান্ডেল করতে হবে।

### 🟡 ৫. একাউন্টিং লেজারের সীমাবদ্ধতা (Lack of True Double-Entry)
- **সমস্যা:** বর্তমানে খরচ (Expense) এবং সেলস আলাদা আলাদা টেবিল আকারে আছে। সাপ্লায়ার বকেয়া বা কাস্টমার বাকির স্বয়ংক্রিয় ডেবিট-ক্রেডিট লেজার নেই।
- **সমাধান:** পূর্ণাঙ্গ একাউন্টিং লেজার (Ledger Entries) সিস্টেম তৈরি করা।

---

## ৫. ব্যাকএন্ড ও বিজনেস লজিক কেমন হওয়া উচিত (Ideal Backend & Architecture Design)

### ৫.১ আইডিয়াল ডেটা আর্কিটেকচার
1. **অ্যাটমিক ট্রানজ্যাকশন (Database Stored Procedure):**
   চালান তৈরি করার সময় একটামাত্র ডাটাবেজ কলে চালান সেভ হবে, স্টক কমবে এবং কাস্টমারের ডিউ (Due) আপডেট হবে। কোনো একটি স্টেপ ফেইল করলে সম্পূর্ণ ট্রানজ্যাকশন স্বয়ংক্রিয়ভাবে রোলব্যাক হবে।

```sql
-- উদাহরণ: অ্যাটমিক চালান কনফার্মেশন ও স্টক ডিডাকশন ফাংশন
CREATE OR REPLACE FUNCTION process_challan_delivery(
    p_challan_id TEXT,
    p_owner_id UUID,
    p_items JSONB,
    p_customer_id TEXT,
    p_total_amount NUMERIC
) RETURNS JSONB AS $$
DECLARE
    item RECORD;
    current_qty NUMERIC;
BEGIN
    -- ১. প্রতিটি প্রোডাক্টের স্টক যাচাই ও বিয়োগ
    FOR item IN SELECT * FROM jsonb_to_recordset(p_items) AS x(product_id TEXT, total_qty NUMERIC) LOOP
        SELECT current_stock INTO current_qty FROM products WHERE id = item.product_id AND owner_id = p_owner_id FOR UPDATE;
        
        IF current_qty < item.total_qty THEN
            RAISE EXCEPTION 'স্টক অপর্যাপ্ত! প্রোডাক্ট আইডি: %', item.product_id;
        END IF;
        
        UPDATE products 
        SET current_stock = current_stock - item.total_qty 
        WHERE id = item.product_id AND owner_id = p_owner_id;
    END LOOP;

    -- ২. কাস্টমারের ডিউ আপডেট
    IF p_customer_id IS NOT NULL AND p_customer_id != '' THEN
        UPDATE customers 
        SET due = COALESCE(due, 0) + p_total_amount 
        WHERE id = p_customer_id AND owner_id = p_owner_id;
    END IF;

    -- ৩. চালানের স্ট্যাটাস ডেলিভার্ড করা
    UPDATE challans 
    SET status = 'Delivered' 
    WHERE id = p_challan_id AND owner_id = p_owner_id;

    RETURN jsonb_build_object('success', true);
END;
$$ LANGUAGE plpgsql;
```

### ৫.২ সার্ভার-সাইড কুয়েরি ও পেজিনেশন (Server-Side Pagination)
- চালানের তালিকা ও হিস্ট্রি একবারে সব না এনে প্রতি পেজে ২০/৫০টি করে ডাটা লোড হবে (`.range(from, to)` কোয়েরি)।
- ফিল্টারিং ও সার্চিং ক্লায়েন্ট মেমোরিতে না করে ডাটাবেজের `WHERE` ক্লজের মাধ্যমে প্রসেস হবে।

---

## ৬. মডিউলভিত্তিক বিস্তারিত রিভিউ (Module-by-Module Audit)

| মডিউল | মূল কাজ | বর্তমান অবস্থা | প্রস্তাবিত উন্নয়ন |
| :--- | :--- | :--- | :--- |
| **১. Dashboard** | প্রতিদিনের সেলস, পারচেজ, প্রফিট এবং ড্যামেজ কেপিআই প্রদর্শন | ✅ সম্পূর্ণ সচল | চার্টগুলোর জন্য সার্ভার-সাইড অ্যাগ্রিগেশন (SQL Views) ব্যবহার করা। |
| **২. Challan Module** | ডেলিভারি অর্ডার তৈরি, এসআর অ্যাসাইনমেন্ট, রিটার্ন ও ড্যামেজ হ্যান্ডেল | ✅ ফিচার ফুলফিল্ড | ক্লায়েন্ট স্টেট ট্রানজ্যাকশন বদলে ডাটাবেজ RPC ফাংশন যুক্ত করা। |
| **৩. Sell / POS Module** | তাৎক্ষণিক সেলস অর্ডার তৈরি ও প্রিন্টিং | ✅ সচল ও সুন্দর | বারকোড স্ক্যানার এবং দ্রুত ক্যাশ সেলস সুবিধা আরও জোরদার করা। |
| **৪. Procurement** | সাপ্লায়ার থেকে মালামাল ক্রয়, ক্যারেজ খরচ ও ইনভেন্টরি এন্ট্রি | ✅ সচল | ক্রয়ের সাথে সাথে সাপ্লায়ারের Payable লেজারে স্বয়ংক্রিয় এন্ট্রি নিশ্চিত করা। |
| **৫. Stock Adjustment** | ড্যামেজ কনভার্শন, লস ও ম্যানুয়াল স্টক কারেকশন | ✅ সচল | অডিট লগের সাথে ইউজার আইডির ডিজিটাল সাইনেচার লক করা। |
| **৬. Claim Management** | কোম্পানির কাছে ড্যামেজ বা ডিসপ্লে ক্লেইম সাবমিট ও ক্রেডিট সেটেলমেন্ট | ✅ চমৎকার লজিক | কোম্পানি সেটেলমেন্ট হলে স্বয়ংক্রিয়ভাবে অ্যাকাউন্টসে ক্রেডিট এন্ট্রি তৈরি হওয়া। |
| **৭. Accounting** | খরচ ব্যবস্থাপনা ও মোট লাভ-ক্ষতি রিপোর্ট | ⚠️ বেসিক পর্যায় | ক্যাশ বুক, ব্যাংক একাউন্ট এবং লেজার রিপোর্ট যোগ করা। |
| **৮. Reports & Analytics** | স্টক হিস্ট্রি, সেলস রিপোর্ট, মার্জিন ও ডে-এন্ড রিপোর্ট | ✅ ভালো ইঞ্জিন | বড় ডেটাসেটের জন্য ব্যাকগ্রাউন্ড CSV এক্সপোর্ট সুবিধা বাড়ানো। |
| **৯. Directory & Personnel** | কোম্পানি, প্রোডাক্ট, এসআর, ডিএসআর, রুট ও কাস্টমার তালিকা | ✅ সম্পূর্ণ সচল | বিশাল ফাইল সাইজ (২৮৯ KB) ছোট ছোট ফাইলে রিফ্যাক্টর করা। |

---

## ৭. ডেটাবেজ ও সিকিউরিটি অপ্টিমাইজেশন প্ল্যান

1. **ইনডেক্সিং (Indexing) নিশ্চিতকরণ:**
   সার্চ ও ফিল্টারিং ফাস্ট করার জন্য নিচের কলামগুলোতে ইনডেক্স থাকতে হবে:
   - `challans(owner_id, created_at, status)`
   - `products(owner_id, company, category_id)`
   - `procurements(owner_id, invoice_date)`
   - `expenses(owner_id, expense_date)`

2. **এনভায়রনমেন্ট ভ্যারিয়েবল ও এপিআই সিকিউরিটি:**
   - `.env.local` ফাইলে থাকা সিক্রেট কি (Resend API Key, Supabase Service Role Key) যেন কখনো ফ্রন্টএন্ড বান্ডেলে এক্সপোজ না হয়।
   - `/api/send-invoice` রুটে রেট-লিমিটিং (Rate Limiting) যুক্ত করা যাতে কেউ স্প্যাম ইমেইল ট্রিগার করতে না পারে।

---

## ৮. পরবর্তী কর্মপরিকল্পনা ও রোডম্যাপ (Implementation Roadmap)

### 🚀 ফেজ ১: ডেটা ইন্টিগ্রিটি ও সিকিউরিটি ফিক্স (উচ্চ অগ্রাধিকার)
- [ ] Supabase-এ `process_challan_delivery` ও `process_procurement` সংক্রান্ত RPC Function তৈরি করা।
- [ ] এসআর (SR) লগইনের জন্য পাসওয়ার্ড হ্যাশিং ও সিকিউর API এন্ডপয়েন্ট তৈরি।
- [ ] কোডবেস থেকে অপ্রয়োজনীয় `.backup` ও `.reconstructed` ফাইলগুলো ক্লিন করা।

### ⚡ ফেজ ২: পারফরম্যান্স ও পেজিনেশন (মাঝারি অগ্রাধিকার)
- [ ] `ChallanModule` এবং `ReportsModule`-এ সার্ভার-সাইড পেজিনেশন ও ডেট ফিল্টারিং চালু করা।
- [ ] বড় কম্পোনেন্টগুলোকে (`DirectoryModule.tsx`, `ChallanModule.tsx`) সাব-কম্পোনেন্টে ভাগ করা।

### 📈 ফেজ ৩: অ্যাডভান্সড ফিচার ও একাউন্টিং লেজার
- [ ] কাস্টমার ও সাপ্লায়ার লেজার (Accounts Receivable / Accounts Payable) যুক্ত করা।
- [ ] Supabase Realtime নোটিফিকেশন যুক্ত করা (এসআর অর্ডার সাবমিট করলে অ্যাডমিনের কাছে তাৎক্ষণিক শব্দ সহ নোটিফিকেশন আসবে)।

---
> **উপসংহার:** প্রজেক্টটির বিজনেস লজিক, UI ফিনিশিং এবং এফএমসিজি ডিলারদের উপযোগী ফাংশনালিটি অত্যন্ত চমৎকার ও বাস্তবসম্মত। উল্লিখিত ব্যাকএন্ড ট্রানজ্যাকশন ও স্কেলেবিলিটি অপ্টিমাইজেশনগুলো সম্পন্ন করলে এটি বাংলাদেশের যেকোনো বড় ডিস্ট্রিবিউটরের জন্য একটি আন্তর্জাতিক মানের নির্ভরযোগ্য সফটওয়্যার হবে।
